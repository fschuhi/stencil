[project]
name = "stencil"
version = "0.1.0"
description = "Templated rendering of project artefacts"
requires-python = ">=3.11"
dependencies = [
    "jinja2",
    "pyyaml",
]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::DeprecationWarning"
]
